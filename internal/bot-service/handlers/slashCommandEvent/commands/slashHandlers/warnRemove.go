package slashHandlers

import (
	"github.com/bwmarrin/discordgo"
)

func HandleSlashWarnRemove(s *discordgo.Session, i *discordgo.InteractionCreate) {

	// warnId := i.ApplicationCommandData().Options[0].StringValue()
	// userId := i.ApplicationCommandData().Options[1].StringValue()

	// TODO

}
